# Histórico de Atividades

| Data | O que foi feito |
|------|-----------------|
| 2026-05-24 | Setup do vault de inventário da garagem: schema de Item (shelf/box/qty/minimum_safe_stock/category/aliases/status), skill `garage-inventory` como SSOT, `inventory.sh` (find/list/low-stock/take/add/new) + `test-inventory.sh` (17 testes verdes), `CONTEXT.md` (glossário), ADR-0001 (TSV/JSON) e ADR-0002 (acesso via script), dashboard Dataview, instalação do plugin Dataview, Base interativa do Obsidian (`Garage Inventory.base`), e princípio SSOT global em `~/.claude/CLAUDE.md`. |
| 2026-05-26 | Populado o inventário com ~12 itens novos (S02/S41/S42/S43): GU10 bulb, Styrofoam/Small Cooler Box, SodaStream, Glass Storage Jar, Intel CPU Cooler, 4 fios 4/6mm, Vulkan Lokring Drain Pump, PPE Kit (Porto Seco); +2 no Small Cooler Box. Reconciliação de taxonomia: ~17 categorias ad-hoc → 8 canônicas (migração via edição manual de `category:`, verificada por grep, 0 órfãos); `CONTEXT.md` atualizado (8 categorias + regra de fronteira Electronics×Hardware). `/review` com 2 agentes Opus levantou 3 achados (relé Songle, qty=metros nos fios, lacuna `set-category` vs ADR-0002); confirmado que remarks no corpo das notas são preservados por take/add. |
| 2026-05-26 | Resolvidos os 3 achados da revisão: (#1) relé Songle mantido em Electronics + regra de fronteira no `CONTEXT.md` esclarecida (relé cru ou módulo = Electronics; válvula/bomba = Hardware); (#2) documentado no `CONTEXT.md` que `qty` de itens contínuos (cabos a granel) conta metros; (#3) adicionado subcomando `set-category` ao `inventory.sh` (valida vocabulário canônico, preserva corpo/demais campos) + 5 testes novos (22 verdes) + workflow "Recategorize" no SKILL.md. |
| 2026-05-26 | Backstop de categoria no `inventory.sh new` (resolve pendência antiga): `new` e `set-category` compartilham `validate_category`, que rejeita categoria fora do vocabulário. Mensagem de erro é **auto-diretiva** — instrui o agente a ler `CONTEXT.md`, reusar canônica ou PEDIR validação ao usuário antes de inventar. `CONTEXT.md` documenta que estender o vocabulário exige atualizar lista + tupla `CANONICAL_CATEGORIES`. +3 testes (25 verdes). |
